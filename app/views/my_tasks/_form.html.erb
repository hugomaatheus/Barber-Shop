<%= form_with(model: my_task, local: true) do |form| %>
  <% if my_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_task.errors.count, "error") %> prohibited this my_task from being saved:</h2>

      <ul>
      <% my_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= form.label :task_name, "Tipo" %>
    <%= form.text_field :task_name, id: :my_task_task_name, class: 'validate' %>
  </div>

  <div class="input-field">
    <%= form.label :valor, "Preço" %>
    <%= form.text_field :valor, id: :my_task_valor, class: 'validate' %>
  </div>

  <div class="actions">
    <button type="submit" class="btn waves-effect waves-light z-depth-4">Salvar</button>
    <%= link_to 'Voltar', my_tasks_path, class: 'btn waves-effect waves-light z-depth-4' %>
  </div>
<% end %>
