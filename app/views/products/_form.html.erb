<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    <%= form.label :nome, "Nome" %>
    <%= form.text_field :nome, id: :product_nome, class: 'validate' %>
  </div>

  <div class="input-field">
    <%= form.label :valor, "Preço" %>
    <%= form.text_field :valor, id: :product_valor, class: 'validate' %>
  </div>

  <div class="actions">
    <button type="submit" class="btn waves-effect waves-light z-depth-4">Salvar</button>
    <%= link_to 'Voltar', products_path, class: 'btn waves-effect waves-light z-depth-4' %>
  </div>
<% end %>
